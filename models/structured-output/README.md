# structured-output/

La salida estructurada exigida al LLM (cuando exista) es exactamente `argos-contracts-scenarios/schemas/recommendation/v1.schema.json` — el mismo contrato que ya valida `services/recommendation`. No se duplica un schema paralelo aquí: duplicarlo sería la forma más fácil de que ambos diverjan.
