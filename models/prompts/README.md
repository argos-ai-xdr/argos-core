# prompts/

Vacío a propósito: no hay `LangGraphEngine` implementado (`services/recommendation/README.md`, ADR-008). Cuando exista, cada prompt versionado vive aquí, referenciado por `EvidenceManifest.prompts` (`argos-control/templates/evidence/evidence-manifest-template.json`) — nunca se almacena el chain-of-thought resultante, solo la plantilla de entrada (ADR-016).
