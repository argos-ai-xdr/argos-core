# evaluation-config/

Qué evalúa `argos-validation` sobre las salidas de `argos-core` vive en `argos-validation/suites/` y `argos-validation/thresholds/`, no aquí — mismo motivo que `structured-output/README.md`: un config paralelo solo crearía una segunda fuente de verdad para divergir de la primera.
